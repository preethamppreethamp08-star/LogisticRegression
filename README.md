# LogisticRegression
This is my first ML project. Built a logistic regression model in Python to predict loan approval outcomes from applicant financial data (income, credit score, loan amount). Covered the full ML workflow — EDA, preprocessing, train/test split, feature scaling, and evaluation via accuracy, precision, recall, and ROC-AUC. 
